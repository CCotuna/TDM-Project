services:
  app:
    build: .docker
    stdin_open: true
    tty: true
    container_name: app.DashboardSweetLikeHoney
    environment:
      NODE_ENV: development
      CHOKIDAR_USEPOLLING: true
      CHOKIDAR_INTERVAL: 100
    ports:
      - "5174:5174"
    expose:
      - "5174"
    volumes:
      - .:/app
      # exclude
      - /app/.git/
    working_dir: /app
    user: 1000:1000